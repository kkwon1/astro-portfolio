---
import Layout from "./Layout.astro";

const { frontmatter } = Astro.props;
---
<Layout title={frontmatter.title}>
  <article class="prose prose-sm text-xs w-full max-w-screen-2xl xl:prose-base">
    <slot />
  </article>
</Layout>